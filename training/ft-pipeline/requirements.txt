# ft-pipeline runtime deps. Install into the `ft-pipeline` conda env (python 3.12).
# INSTALL TORCH FIRST, from the CUDA index — see README "Setup". Order matters:
# the default PyPI torch wheel is not the GPU build we want, so pin it up front
# and the `torch` line below is then already satisfied and left alone.
#   conda create -n ft-pipeline python=3.12 -y
#   conda activate ft-pipeline
#   pip install torch --index-url https://download.pytorch.org/whl/cu130
#   pip install -r requirements.txt
#
# Core (needed for every stage, even the `noop`/`stub` walking skeleton):
pyyaml>=6.0

# `train.backend: peft` / `predict` — real LoRA training + generation:
torch
transformers>=4.46
peft>=0.13
accelerate>=1.0
datasets>=3.0
huggingface_hub

# `evaluate.graders: [..., reference_overlap]` — ROUGE-L / BLEU on top of the
# always-available token_f1. Optional: reference_overlap runs without these,
# it just skips rougeL/bleu and warns once.
rouge-score
sacrebleu

# `evaluate.graders: [..., llm_judge]` — opt-in judge grader. Only the
# provider you actually configure needs an API key at runtime.
anthropic
openai
